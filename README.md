1. RAG system for pdf documents using Ollama models
2. Application run on CPU and llm models are in remote GPU
3. Uses sentence based chunking using nltk, and chromadb for vector database
4. Installation Instructions:
  4.1. pip install -r requirements.txt
  4.2. run 'streamlit run ./pdf_rag_remotegpu_streamlit.py'
